# Built output — regenerated by `node build.mjs`, deployed by CI. Not committed.
dist/
node_modules/
.DS_Store
# The tracker's three public client keys for local testing (build.mjs); CI uses secrets.
config.local.json
