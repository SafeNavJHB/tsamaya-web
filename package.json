{
  "name": "tsamaya-website",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing + info website for Tsamaya (Go well.) — static site, zero runtime dependencies.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node serve.mjs",
    "dev": "node build.mjs && node serve.mjs",
    "stats": "node scripts/fetch-stats.mjs",
    "changelog": "node scripts/sync-changelog.mjs",
    "images": "node scripts/optimise-images.mjs",
    "check": "node scripts/check-seo.mjs"
  },
  "//": "The BUILD has no dependencies — `node build.mjs` runs on a clean checkout with no npm install, which is why CI needs nothing. sharp is used only by `npm run images`, a manual step whose output is committed. Never move it to dependencies.",
  "devDependencies": {
    "sharp": "^0.34.2"
  },
  "engines": {
    "node": ">=18"
  }
}
